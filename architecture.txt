- I need a way to programatically create redirects (So a class for creating, removing, storing in a non volatile way, looking up, etc) redirects
- I need to integrate that with Flask to make them actually work
- I need to provide a way to administrate those redirects